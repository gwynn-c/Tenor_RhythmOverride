using Interfaces;
using UnityEngine;

public class ReactiveElement : MonoBehaviour, IReactable
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void ReactToSoundTrack()
    {
        //LeanTween Implement to size 
    }

    public void ReactToOnBeat()
    {
        //Using the Feel package create reactive animations
    }
}
